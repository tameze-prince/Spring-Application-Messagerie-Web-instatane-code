# Spring backend REST base URL (without a trailing slash)
NEXT_PUBLIC_API_URL=http://localhost:8080/api/v1
# Spring backend WebSocket endpoint, consumed when wiring the authenticated app shell
NEXT_PUBLIC_WS_URL=ws://localhost:8080/ws
